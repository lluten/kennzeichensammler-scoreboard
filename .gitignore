# Python
__pycache__/
*.pyc

# Local decode scratch
.tmp_decode.sqlite
decoded_payload.bin
decoded_payload.sqlite

# Prototype + sample Drive uploads (not needed for Pages)
example/

# Loose sample backups in repo root
KennzeichensammlerBackupX-*

# Local raw backups — Apps Script / Drive push owns remote copies
backups/*/
!backups/.gitkeep
